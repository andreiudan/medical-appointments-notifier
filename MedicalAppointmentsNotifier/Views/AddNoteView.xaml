<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MedicalAppointmentsNotifier.Views.AddNoteView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedicalAppointmentsNotifier.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Adauga Mentiune">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid 
        x:Name="RootGrid" 
        VerticalAlignment="Center" 
        RowSpacing="18"
        Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Adauga Mentiune" FontSize="28" FontWeight="SemiBold" Grid.Row="0" HorizontalAlignment="Center"/>
                
        <TextBox 
            Grid.Row="1" 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch"
            Header="Descriere"
            Text="{x:Bind ViewModel.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            />
        <TextBlock
            Foreground="{ThemeResource SystemErrorTextColor}"
            Text="{x:Bind ViewModel.DescriptionErrorMessage, Mode=OneWay}"/>

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>

            <CalendarDatePicker 
                PlaceholderText="Alege o data" 
                Header="Din data" Grid.Column="0" 
                HorizontalAlignment="Left"
                Date="{x:Bind ViewModel.DateFrom, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                DateFormat="{}{day.integer}/{month.integer}/{year.full}"/>
            <CalendarDatePicker 
                PlaceholderText="Alege o data" 
                Header="Pana in data" Grid.Column="1" 
                HorizontalAlignment="Right"
                Date="{x:Bind ViewModel.DateTo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                DateFormat="{}{day.integer}/{month.integer}/{year.full}"/>
        </Grid>

        <Grid Grid.Row="3" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="btnClose" Content="Anuleaza" Grid.Column="0" Click="btnClose_Click"/>
            <Button Content="Adauga" Grid.Column="1" HorizontalAlignment="Right" Command="{x:Bind ViewModel.AddNoteCommand}"/>
        </Grid>
    </Grid>
</Window>
