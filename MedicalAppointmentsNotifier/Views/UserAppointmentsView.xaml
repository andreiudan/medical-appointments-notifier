<?xml version="1.0" encoding="utf-8"?>
<Page x:Name="RootPage"
    x:Class="MedicalAppointmentsNotifier.Views.UserAppointmentsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedicalAppointmentsNotifier.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:c="using:MedicalAppointmentsNotifier.Converters"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    mc:Ignorable="d">
    <Page.Resources>
        <c:DateOnlyConverter x:Key="DateOnlyConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="55*"/>
        </Grid.RowDefinitions>

        <Grid 
            Grid.Row="0"
            Padding="36, 32, 36, 24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button 
                x:Name="btnBack" 
                Grid.Column="0" 
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Click="btnBack_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Back" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="Inapoi" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <TextBlock 
                Text="{x:Bind ViewModel.UserName}"
                Grid.Column="1"
                FontSize="32" 
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Style="{StaticResource TitleTextBlockStyle}"/>
        </Grid>
        

        <Grid 
            Grid.Row="1"
            Padding="36, 0, 36, 12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock 
                Text="Mentiuni" 
                FontSize="18" 
                Grid.Column="0" 
                HorizontalAlignment="Left" 
                VerticalAlignment="Center" 
                Style="{StaticResource SubtitleTextBlockStyle}"/>
            <StackPanel 
                Orientation="Horizontal" 
                Grid.Column="1" 
                HorizontalAlignment="Right" 
                Spacing="8">
                <Button 
                    x:Name="btnAddNote" 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Click="btnAddNote_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Add" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Adauga" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Command="{x:Bind ViewModel.DeleteNotesCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Delete" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Sterge" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <ListView
            Margin="36,0,36,16"
            x:Name="lvNotes"
            Grid.Row="2" 
            BorderThickness="1"
            CornerRadius="{StaticResource ControlCornerRadius}"
            BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
            ItemsSource="{x:Bind ViewModel.Notes}">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid 
                        Padding="14, 12, 14, 12"
                        Margin="0, 0, 0, 4"
                        ColumnSpacing="12"
                        Background="{ThemeResource SolidBackgroundFillColorQuarternaryBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="60*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="8*"/>
                        </Grid.ColumnDefinitions>

                        <interactivity:Interaction.Behaviors>
                            <behaviors:StickyHeaderBehavior/>
                        </interactivity:Interaction.Behaviors>

                        <TextBlock Text="DEL" Grid.Column="0" HorizontalTextAlignment="Left" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Descriere" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Din Data" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Pana in Data" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Modifica" Grid.Column="4" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid 
                        Margin="0,8,0,8"
                        Padding="0,6" 
                        ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="60*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="8*"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="0" Click="CheckBox_Click"/>
                        <TextBlock Text="{Binding Description}" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <TextBlock Text="{Binding From, Converter={StaticResource DateOnlyConverter}}" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <TextBlock Text="{Binding Until, Converter={StaticResource DateOnlyConverter}}" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <Button
                            Grid.Column="4"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            x:Name="btnNoteEdit">
                            <SymbolIcon Symbol="Edit"/>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid 
            Grid.Row="3"
            Padding="36, 0, 36, 12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock 
                Text="Scrisori Medicale"  
                FontSize="18" 
                Grid.Column="0" 
                HorizontalAlignment="Left" 
                VerticalAlignment="Center"
                Style="{StaticResource SubtitleTextBlockStyle}"/>
            <StackPanel 
                Orientation="Horizontal" 
                Grid.Column="1" 
                HorizontalAlignment="Right" 
                Spacing="8">
                <Button x:Name="btnAddAppointment" Click="btnAddAppointment_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Add" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Adauga" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Command="{x:Bind ViewModel.DeleteAppointmentsCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Delete" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Sterge" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <ListView 
            Margin="36, 0, 36, 24"
            x:Name="lvAppointments"
            BorderThickness="1" 
            BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
            Grid.Row="4"
            CornerRadius="{StaticResource ControlCornerRadius}"
            ItemsSource="{x:Bind ViewModel.Appointments}"
            Loaded="lvAppointments_Loaded">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid
                        Padding="14, 12, 14, 12"
                        Margin="0, 0, 0, 4"
                        ColumnSpacing="12"
                        Background="{ThemeResource SolidBackgroundFillColorQuarternaryBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5*"/>
                            <ColumnDefinition Width="20*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="20*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="8*"/>
                        </Grid.ColumnDefinitions>

                        <interactivity:Interaction.Behaviors>
                            <behaviors:StickyHeaderBehavior/>
                        </interactivity:Interaction.Behaviors>

                        <TextBlock Text="DEL" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Specialitate" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Interval" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Zile ramase" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Status" Grid.Column="4" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Din Data" Grid.Column="5" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Pana in Data" Grid.Column="6" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Modifica" Grid.Column="7" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>

            <ListView.ItemTemplate>
                <DataTemplate ScrollViewer.IsVerticalRailEnabled="True">
                    <Grid 
                        Margin="0,8,0,8"
                        Padding="0,6"
                        ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5*"/>
                            <ColumnDefinition Width="20*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="20*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="8*"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="0" Click="CheckBox_Click"/>
                        <TextBlock Text="{Binding MedicalSpecialty}" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <TextBlock Text="{Binding IntervalDays}" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <TextBlock Text="{Binding DaysUntilNextAppointment}" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <ComboBox
                            ItemsSource="{Binding DataContext.AppointmentStatuses, ElementName=RootPage}"
                            SelectedItem="{Binding Status, Mode=TwoWay}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Grid.Column="4"
                            SelectionChanged="ComboBox_SelectionChanged">
                        </ComboBox>
                        <TextBlock Text="{Binding LatestDate, Converter={StaticResource DateOnlyConverter}}" Grid.Column="5" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <TextBlock Text="{Binding NextDate, Converter={StaticResource DateOnlyConverter}}" Grid.Column="6" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <Button
                            x:Name="btnAppointmentEdit"
                            Grid.Column="7"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <SymbolIcon Symbol="Edit"/>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
