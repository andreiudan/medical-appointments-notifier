<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MedicalAppointmentsNotifier.Views.UsersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedicalAppointmentsNotifier.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MedicalAppointmentsNotifier.Core.ViewModels" 
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity" 
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid 
            VerticalAlignment="Top"
            Padding="36, 40, 36, 24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock 
                Grid.Column="0" 
                Text="Beneficiari" 
                HorizontalAlignment="Left" 
                FontSize="32" 
                Style="{StaticResource TitleTextBlockStyle}"/>
            <StackPanel 
                Grid.Column="1" 
                Orientation="Horizontal" 
                HorizontalAlignment="Right"
                Spacing="8">
                <Button 
                    x:Name="btnAdd"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" 
                     Click="btnAdd_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Add" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Adauga" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" 
                    Command="{x:Bind ViewModel.DeleteSelectedUsersCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Delete" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Text="Sterge" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </Grid>

        <ListView 
            BorderThickness="1" 
            BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
            CornerRadius="{StaticResource ControlCornerRadius}"
            Margin="36,0,36,0"
            Grid.Row="1" 
            x:Name="UsersListView"
            ItemsSource="{x:Bind ViewModel.Users}"
            IsItemClickEnabled="True"
            ItemClick="UsersListView_ItemClick">
            
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid 
                        Padding="14, 12, 14, 12"
                        Margin="0, 0, 0, 4"
                        Background="{ThemeResource SolidBackgroundFillColorQuarternaryBrush}"
                        ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="40*"/>
                            <ColumnDefinition Width="35*"/>
                            <ColumnDefinition Width="25*"/>
                        </Grid.ColumnDefinitions>

                        <interactivity:Interaction.Behaviors>
                            <behaviors:StickyHeaderBehavior/>
                        </interactivity:Interaction.Behaviors>

                        <TextBlock Text="DEL" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Nume" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Urmatoarea programare (zile)" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Status" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid 
                        Margin="0,8,0,8"
                        Padding="0,6"
                        ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="40*"/>
                            <ColumnDefinition Width="35*"/>
                            <ColumnDefinition Width="25*"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox 
                            IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                            HorizontalAlignment="Left" 
                            VerticalAlignment="Center" 
                            Grid.Column="0" 
                            Click="CheckBox_Click"/>
                        <TextBlock Text="{Binding Name}" Grid.Column="1" HorizontalTextAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <TextBlock Text="{Binding DaysUntilNextAppointment}" Grid.Column="2" HorizontalTextAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                        <TextBlock Text="{Binding Status}" Grid.Column="3" HorizontalTextAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyTextBlockStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel 
            Orientation="Horizontal" 
            HorizontalAlignment="Left" 
            VerticalAlignment="Center" 
            Grid.Row="2" 
            Spacing="12"
            Padding="36,24,36,24">
            <TextBlock Text="Urmeaza sa expire" FontSize="16"></TextBlock>
            <TextBlock Text="A expirat" FontSize="16"></TextBlock>
            <TextBlock Text="Nu expira curand" FontSize="16"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
