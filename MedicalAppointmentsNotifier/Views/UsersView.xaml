<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MedicalAppointmentsNotifier.Views.UsersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedicalAppointmentsNotifier.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MedicalAppointmentsNotifier.Core.ViewModels"
    mc:Ignorable="d">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        
        <Grid VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Beneficiari" HorizontalAlignment="Left" FontSize="32" FontWeight="Bold" Margin="10"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Add User" Margin="5"/>
                <Button Content="Delete User" Margin="5"/>
            </StackPanel>
        </Grid>

        <ListView 
            BorderThickness="1" 
            BorderBrush="DarkRed" 
            Margin="10" 
            Grid.Row="1" 
            x:Name="UsersListView"
            ItemsSource="{x:Bind ViewModel.Users}"
            IsItemClickEnabled="True"
            ItemClick="UsersListView_ItemClick">
            
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40*"/>
                            <ColumnDefinition Width="35*"/>
                            <ColumnDefinition Width="25*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Text="Nume" Grid.Column="0" HorizontalAlignment="Center" />
                        <TextBlock Text="Urmatoarea programare" Grid.Column="1" HorizontalAlignment="Center"/>
                        <TextBlock Text="Status" Grid.Column="2" HorizontalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>

            <ListView.ItemTemplate>
                <DataTemplate ScrollViewer.IsVerticalRailEnabled="True">
                    <Grid Padding="16" BorderThickness="1" BorderBrush="{ThemeResource AccentAAFillColorDefaultBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40*"/>
                            <ColumnDefinition Width="35*"/>
                            <ColumnDefinition Width="25*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Name}" Width="200" Grid.Column="0" HorizontalTextAlignment="Center"/>
                        <TextBlock Text="A" Width="200" Grid.Column="1" HorizontalTextAlignment="Center"/>
                        <TextBlock Text="B" Width="150" Grid.Column="2" HorizontalTextAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Row="2" Spacing="12">
            <TextBlock Text="Urmeaza sa expire" FontSize="16"></TextBlock>
            <TextBlock Text="A expirat" FontSize="16"></TextBlock>
            <TextBlock Text="Nu expira curand" FontSize="16"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
