<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MedicalAppointmentsNotifier.Views.AddUserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedicalAppointmentsNotifier.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MedicalAppointmentsNotifier.Core.ViewModels"
    mc:Ignorable="d"
    Title="Adauga Beneficiar">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid 
        x:Name="RootGrid"
        HorizontalAlignment="Stretch" 
        VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock 
            Text="Adauga Beneficiar" 
            FontSize="28" 
            Style="{StaticResource TitleTextBlockStyle}"
            Grid.Row="0" 
            HorizontalAlignment="Center"
            Padding="32, 32, 32, 24"/>

        <StackPanel Grid.Row="1" Padding="32, 0, 32, 16">
            <TextBox 
                Header="Prenume"  
                Text="{x:Bind ViewModel.FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock
                Foreground="{ThemeResource SystemControlErrorTextForegroundBrush}"
                Text="{x:Bind ViewModel.FirstNameErrorMessage, Mode=OneWay}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Padding="32, 0, 32, 24">
            <TextBox 
                Header="Nume de familie"
                Text="{x:Bind ViewModel.LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock
                Foreground="{ThemeResource SystemControlErrorTextForegroundBrush}"
                Text="{x:Bind ViewModel.LastNameErrorMessage, Mode=OneWay}"/>
        </StackPanel>

        <Grid 
            Grid.Row="3"
            ColumnSpacing="8" 
            Padding="32, 24, 32, 24"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>
            
            <Button 
                x:Name="btnClose" 
                Content="Anuleaza" 
                Click="btnClose_Click"  
                Grid.Column="0" 
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Center"/>
            <Button 
                Content="Adauga" 
                Command="{x:Bind ViewModel.AddUserCommand}" 
                Grid.Column="1" 
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
