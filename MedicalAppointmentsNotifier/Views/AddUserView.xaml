<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MedicalAppointmentsNotifier.Views.AddUserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedicalAppointmentsNotifier.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MedicalAppointmentsNotifier.Core.ViewModels"
    mc:Ignorable="d"
    Title="AddUserView">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" HorizontalAlignment="Center" VerticalAlignment="Center" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Adauga Beneficiar" FontSize="28" FontWeight="Bold" Grid.Row="0" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1">
            <TextBox 
                Header="Prenume"  
                Text="{x:Bind ViewModel.FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock
                Foreground="{ThemeResource SystemControlErrorTextForegroundBrush}"
                Text="{x:Bind ViewModel.FirstNameErrorMessage, Mode=OneWay}"
                Visibility="{x:Bind Visible}"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBox 
                Header="Nume de familie" 
                Text="{x:Bind ViewModel.LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock
                Foreground="{ThemeResource SystemControlErrorTextForegroundBrush}"
                Text="{x:Bind ViewModel.LastNameErrorMessage, Mode=OneWay}"
                Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="12" Grid.Row="3">
            <Button x:Name="btnClose" Content="Anuleaza" Click="btnClose_Click"/>
            <Button Content="Adauga" Command="{x:Bind ViewModel.AddUserCommand}"/>
        </StackPanel>
    </Grid>
</Window>
